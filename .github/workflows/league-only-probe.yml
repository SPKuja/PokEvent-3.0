name: League-Only Source Probe

on:
  push:
    branches:
      - feature/play-pokemon-ingestion
    paths:
      - "scripts/probe_league_only.py"
      - ".github/workflows/league-only-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  swindon-league:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: python -m pip install -e .

      - name: Probe League 2012924 without location
        run: python scripts/probe_league_only.py
